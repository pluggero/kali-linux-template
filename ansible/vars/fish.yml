---
fish_enable_vim_mode: true
fish_env_variables:
  - name: GPG_TTY # Fix GPG with fish shell
    value: "(tty)"
  - name: XCURSOR_SIZE
    value: "22"
  - name: XCURSOR_THEME
    value: "Bibata-Modern-Ice"
  - name: "WINIT_X11_SCALE_FACTOR" # To fix DPI resolution in alacritty
    value: "1"
fish_abbreviations:
  - name: l
    value: "ls -lah"
fish_keybindings:
  - key: \co
    function: "clear_and_repaint"
fish_functions:
  - function:
    name: y
    description: "yazi with cwd-file."
    command: |
      set tmp (mktemp -t "yazi-cwd.XXXXXX")
      yazi $argv --cwd-file="$tmp"
      if set cwd (command cat -- "$tmp"); and [ -n "$cwd" ]; and [ "$cwd" != "$PWD" ]
        builtin cd -- "$cwd"
      end
      rm -f -- "$tmp"
